POSTGRES_USER=USER
POSTGRES_PASSWORD="PASS"
POSTGRES_DB=pymap
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DJANGO_ENV="production"
RABBITMQ_DEFAULT_USER=pymap
RABBITMQ_DEFAULT_PASS="PASS"
DJANGO_SETTINGS_MODULE=pymap.settings
CELERY_RESULT_BACKEND=django-db
STATIC_ROOT=/var/www/static # /home/pymap/static in development, do not use app folder in dev gets ovverriden
PYMAP_LOGDIR=/var/log/pymap # pymap_logs in development
PYMAP_HOSTNAME=pymap.lan
GROUPNAME=pymap
GID=1001

WORKER_REPLICAS=2 # Optional, defaults to 1
FLOWER_ADMIN="USER" # Optional, only used in flower profile
FLOWER_PASSWORD="PASS" # Optional, only used in flower profile
WORKER_LOG_LEVEL="INFO" # Optional, defaults to INFO
BEAT_LOG_LEVEL="INFO" # Optional, defaults to INFO